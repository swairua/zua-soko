{
  "buildCommand": "cd frontend && npm ci && npm run build:prod && cp -r dist/* ../",
  "outputDirectory": ".",
  "installCommand": "npm ci && cd frontend && npm ci",
  "functions": {
    "api/index.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
