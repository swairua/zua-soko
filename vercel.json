{
  "buildCommand": "cd frontend && npm install && npm run build:prod && (test -f dist/index.production.html && mv dist/index.production.html dist/index.html || echo 'Using default index.html') && cp -r dist/* ../",
  "outputDirectory": ".",
  "installCommand": "npm install && cd frontend && npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
